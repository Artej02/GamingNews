@model int?
@using Kendo.Mvc.UI

@(Html.Kendo().DropDownTreeFor(m => m)
    .ValuePrimitive(true)
    .BindTo((IEnumerable<DropDownTreeItemModel>)ViewBag.Organizations)
    .Events(e => e.DataBound("onDataBound"))
)
