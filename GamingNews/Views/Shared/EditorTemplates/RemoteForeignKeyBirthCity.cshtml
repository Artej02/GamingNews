@using Kendo.Mvc.UI
@(Html.Kendo().DropDownListFor(m => m)
	 .DataValueField("BirthCityID")
	 .AutoBind(false)
  .DataSource(source =>
  {
	  source.Read(read =>
	  {
		  read.Action("GetCities", "Users").Type(HttpVerbs.Post).Data("dataFuncBirthCity");
	  }).ServerFiltering(true);
  })
  .DataValueField("Key")
  .DataTextField("Value")
  .CascadeFrom("BirthCountryID")
)